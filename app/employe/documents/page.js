"use client";
import Documentation from "@/app/documentation/page";

// Documents de l'employé — réutilise la rubrique « Mes documents »
// (dépôt Supabase Storage rattaché au compte), dans l'habillage employé.
export default function DocumentsEmploye() {
  return <Documentation />;
}
